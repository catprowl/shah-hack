import React from 'react';
import ReactDOM from 'react-dom';
import Services from './services'; // Assuming your Services component is in services.js

ReactDOM.render(
  <React.StrictMode>
    <Services />
  </React.StrictMode>,
  document.getElementById('root')
);
